type MessageType {
    id: ID!
    name: String!
    created_at: DateTime!
    updated_at: DateTime!
    deleted_at: DateTime
}

extend type Query {
    messageTypes: [MessageType!]! @paginate(defaultCount: 15)
    messageType(id: ID @eq): MessageType @find
}